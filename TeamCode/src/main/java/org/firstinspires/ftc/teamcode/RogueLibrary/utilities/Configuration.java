package org.firstinspires.ftc.teamcode.RogueLibrary.utilities;

//import com.rogue.mafia.ftc.roguelibrary.drives.IDriveBase;

import java.util.Collections;
import java.util.HashMap;
import java.util.Map;

/**
 * Hardcoded layer of hardware device configuration to be implemented by user for their unique robot.
 */
public abstract class Configuration {
    /*
    /**
     * Provides a reference for DriveBases to the name of the configured drive motors.
     * Creates a layer of abstraction beyond the already abstracted process of configuring a robot.
     */
    //HashMap<IDriveBase.DriveMotorPosition, String> driveMotorNames = new HashMap<IDriveBase.DriveMotorPosition, String>();


    /**
     * Produces an unmodifiable map mapping {@link IDriveBase.DriveMotorPosition} with the corresponding string for configuration.
     * @return Unmodifiable Map with Motor Positions linked to a String
     */
    /*public final Map<IDriveBase.DriveMotorPosition, String> getDriveMotorNames() {
        return Collections.unmodifiableMap(driveMotorNames);
    }
    */
}
